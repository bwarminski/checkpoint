# JOURNAL

- 2026-04-03: Initialized repo skeleton plan execution journal.
- 2026-04-03: Added the Task 1 scaffold for docker compose, Postgres bootstrap SQL, and service package files.
- 2026-04-03: Used a minimal valid `agent/package.json` because JSON does not support comments.
- 2026-04-03: Installed `python3-pytest` via `apt-get` so the required smoke test could run in this environment.
- 2026-04-03: Verified `docker compose config` renders the compose file without syntax errors.
- 2026-04-03: Mounted `postgres/init` into `/docker-entrypoint-initdb.d` so the bootstrap SQL runs in the Postgres container.
- 2026-04-03: Switched the smoke test to derive the repo root from the test file location.
- 2026-04-03: Added `shared_preload_libraries=pg_stat_statements` to the Postgres service command so the extension bootstrap is viable on first startup.
- 2026-04-03: Strengthened the smoke test to assert the exact init bind mount entry.
- 2026-04-04: Built the minimal Rails demo app for Task 2 with todo routes, controller actions, models, schema, seeds, and controller integration coverage.
- 2026-04-04: Added the smallest extra Rails support files not listed in the plan so `bundle exec rails test` and `bundle exec rails db:setup` can run: `config/boot.rb`, `config/environment.rb`, `config/environments/test.rb`, `config/database.yml`, `app/controllers/application_controller.rb`, `app/models/application_record.rb`, `test/test_helper.rb`, `bin/rails`, `Rakefile`, and `demo/.gitignore`.
- 2026-04-04: Loaded `db/schema.rb` and seeds from `demo/test/test_helper.rb` because the approved Task 2 test expects rows in the response body without creating fixtures in the test itself.
- 2026-04-04: Pinned `minitest` to `~> 5.25` because Rails 7.1.6 resolved against Minitest 6 in this environment and the Rails test runner failed with an argument mismatch.
- 2026-04-04: Changed `/todos/stats` to key its JSON response by stringified user IDs so the payload shape is stable across renders.
- 2026-04-04: Strengthened the Task 2 controller test to parse JSON and assert row counts, filtered results, `/todos/status`, and `/todos/stats` response shape directly.
- 2026-04-04: Added the Task 3 ClickHouse schema files and the Ruby schema test under `collector/test/sql`.
- 2026-04-04: Pinned `collector`'s `minitest` dependency to `5.16.3` because that version is available locally through Bundler in this environment.
- 2026-04-04: Validated the ClickHouse DDLs by streaming the local SQL files into `clickhouse-client` because this compose setup does not mount the repository at `/workdir/collector`.
- 2026-04-04: Made the Task 3 SQL test resolve files from `__dir__` so it works outside `collector/`.
- 2026-04-04: Replaced the three independent `anyState` fields with a single `argMaxState` tuple keyed by `collected_at` so the representative row stays coherent.
- 2026-04-04: Validated the updated ClickHouse schema by recreating the destination objects in the running ClickHouse container.
- 2026-04-04: Built the Task 4 Ruby collector scaffold with `QueryCommentParser`, `SampleQueryLookup`, a minimal `Collector#run_once`, and the `collector/bin/collector` entrypoint.
- 2026-04-04: Kept the collector runtime wiring out of scope for Task 4; the executable only loads the collector and runs a no-op pass until later tasks provide real connections.
- 2026-04-04: Used the sampled `pg_stat_activity` query text, not the normalized `pg_stat_statements` text, as the source for Rails comment parsing so collector rows can capture `source_tag` and `source_file`.
- 2026-04-04: No extra collector gems or support files were needed for Task 4 beyond the planned Ruby files and tests.
- 2026-04-04: Tightened collector comment extraction to scan single `/* ... */` blocks and pick the Rails metadata block explicitly so trailing or leading block comments do not corrupt `source_file`.
- 2026-04-04: Built the Task 5 load harness with a fixed round-robin endpoint list and a simple `run` loop that sleeps at the requested rate between requests.
- 2026-04-04: Verified the harness request order against a temporary local TCP server because the repo stack does not include a runnable demo HTTP server or collector pipeline in this environment.
- 2026-04-04: Added a blocking-correction smoke test that requires compose to define `demo` and `collector`, and requires the runnable demo path to use Postgres.
- 2026-04-04: Wired the demo and collector into compose with minimal Dockerfiles, kept the test suite on SQLite, and added `pg` where the real runtime path needs it.
- 2026-04-04: The first compose verification exposed two more missing runtime prerequisites: the Rails image needed `puma` plus `config.ru`, and ClickHouse needed a user config that permits requests from sibling containers.
- 2026-04-04: Task 6 uses `tsx` from `agent/package.json` for `start` and `test` because the local Node runtime is `v18.19.1` and cannot run TypeScript files directly.
- 2026-04-04: Pinned `agent`'s `@a2a-js/sdk` dependency to the exact version `0.3.13` and verified the install with `npm ls @a2a-js/sdk`.
- 2026-04-04: Added the Gate A agent scaffold: `DBSpecialistExecutor`, the ExplainTool SELECT-only guard, and an Express A2A server wired through `DefaultRequestHandler`, JSON-RPC, REST, and a health endpoint.
- 2026-04-04: Left `CodeSearchTool` transport-neutral and effectively stubbed because `pi-agent-core` external MCP client support is unverified and no local package or repo reference confirms it.
- 2026-04-04: Built the Step 6b demo MCP server in `agent/src/mcp-servers/code-search/` with stdio transport, `read_file`, and `search_code`, backed by `CODE_SEARCH_ROOT`.
- 2026-04-04: Wired Step 6c mcporter generation through `agent/.mcporter.json` and `npm run generate:code-search`, and kept generated artifacts out of git via `agent/.gitignore`.
- 2026-04-04: Ran mcporter through `bun ./node_modules/mcporter/dist/cli.js` instead of the package bin because `mcporter@0.8.1` now expects Node >=20.11 while this workspace is on Node 18.19; the direct Bun invocation generated the client successfully.
- 2026-04-04: Implemented `CodeSearchTool` to strip the `/app/` container prefix, pass repo-relative `file:line` references to the generated client, and return the captured file context.
- 2026-04-04: Implemented Step 7 memory schema with `findings`, `suggestions`, and `pattern_log`, and made `MemoryTool` block `pending`, `accepted`, and `rejected` suggestions while allowing stale `invalid` suggestions to retry after a caller-provided cooldown.
- 2026-04-04: Excluded the generated mcporter client from `tsc`'s static program and loaded it through a non-literal dynamic import because the current emitted file is not NodeNext-clean under this TypeScript setup, even though runtime generation works.
- 2026-04-04: Follow-up on `17ddb03`: the code-search path mapping now preserves the leading `app/` segment for `CODE_SEARCH_ROOT=../demo`, rejects absolute paths outside `/app/`, rejects `:0` line references explicitly, and keeps generated-client startup lazy with an explicit `close()` cleanup path.
- 2026-04-04: Final Step 6b-7 follow-up: failed lazy code-search client initialization now clears its cached promise for retries, and the generate script patches mcporter's emitted runtime wrapper so the positional `read_file(path, lines?)` / `search_code(pattern, glob?)` contract is honored at runtime.
- 2026-04-04: Implemented Step 8 in `DBSpecialistExecutor`: it now reads A2A text parts, loads top offenders, traces source context, proposes a minimal `add_index` fix, runs validation via the injected explain boundary, and opens a PR only when severity is `high`, validation returns `validated: true`, and memory does not block the fingerprint + fix type.
- 2026-04-04: Switched `agent` tests to `node --import tsx --test ...` because the `tsx` CLI opens an IPC pipe that this sandbox denies; the Node test runner path stays inside the sandbox and still executes the TypeScript tests correctly.
- 2026-04-04: Step 8 follow-up root cause: `@a2a-js/sdk` only returns a task result after the executor publishes an initial `task` event, so status updates alone were not enough for live `message/send`.
- 2026-04-04: Fixed the Step 8 live A2A contract by publishing a submitted task before working/completed updates and by carrying findings in the completed task message payload.
- 2026-04-04: Brought the agent test command back in sync with the current `test/integration/` layout and added `analyze_table` to the advertised agent card because the server test and live card surface require both skills.
- 2026-04-04: Step 8b root cause on the collector side was twofold: the live Rails SQL comments use `controller='todos'` / `action='index'` formatting, not the earlier colon form, and second-resolution `collected_at` values let `argMaxState(..., collected_at)` pick null representative rows when multiple samples landed in the same second.
- 2026-04-04: Fixed the live collector path by teaching comment parsing to accept both `key:value` and `key='value'` formats, by recognizing both marker styles in the collector, and by moving ClickHouse event timestamps to `DateTime64(3)` with millisecond JSON serialization.
- 2026-04-04: Wired the default agent runtime to the real local services: ClickHouse offender reads over HTTP, Postgres-backed `EXPLAIN`, Postgres-backed memory schema bootstrap, code search via the generated MCP client, and a local demo PR fallback when no GitHub client is configured.
- 2026-04-04: The live Step 8b proof used `curl` against `http://127.0.0.1:3001/a2a/jsonrpc` with `message/send analyze_db` and returned one completed finding with fingerprint `-5767027640429317262`, traced source `app/controllers/todos_controller.rb:1`, real `EXPLAIN` plan rows, and a demo PR URL `local://db-specialist/pull-requests/-5767027640429317262`.
- 2026-04-04: Added repo ignores for `collector/.bundle/` and `collector/vendor/` because the local Bundler install path is generated runtime state and should not be committed.
- 2026-04-04: Step 9 adds a minimal blog skeleton under `docs/blog/` that explicitly calls out the A2A flow, EXPLAIN plan diff, and HypoPG so the public write-up starts from the Phase 1 proof instead of a blank page.
- 2026-04-04: Step 10 starts the Phase 2 transport work with a minimal `RedpandaConsumer` payload normalizer, adds a Redpanda service to compose, and extends the collector image with `librdkafka-dev` while leaving the ClickHouse read model unchanged.
- 2026-04-04: Started the demo-repo split by defining `DEMO_APP_ROOT` and related GitHub env vars in `.env.example`, rewiring compose and mcporter to the sibling demo path, and planning to make `/home/bjw/db-specialist-demo` the single source of truth for the Rails app.
- 2026-04-04: Finished the sibling demo repo split by treating `/home/bjw/db-specialist-demo` as the only Rails app source of truth, documenting the required `DEMO_APP_ROOT`, `DEMO_REPO`, `DEMO_BASE_REF`, `DEMO_HEAD_REF`, and `GITHUB_TOKEN` session variables in the root `README.md`, and keeping `TODOS.md` untouched as deferred context.
- 2026-04-04: Replaced the placeholder fix classifier with deterministic source-based classification in `DBSpecialistExecutor`: leading-wildcard `LIKE` becomes `rewrite_like`, looped `count` becomes `rewrite_count`, association access patterns become `add_includes`, and equality filters fall back to `add_index`.
- 2026-04-04: Moved the default ClickHouse time-window query to `query_events` with a 60-minute window and kept `query_fingerprints` only for explicit all-time analysis, because the aggregated table has no event-time column to filter on safely.
- 2026-04-04: Updated ClickHouse offender ranking to sort by `total_exec_time_ms` and severity to key off `p95_exec_time_ms >= 100`, with targeted tests covering both the time-windowed and all-time query paths.
- 2026-04-04: Corrected the ClickHouse schema root cause by adding `total_exec_time_ms_state` to `query_fingerprints` and emitting it from `top_offenders_mv`, so the all-time ranking uses a real aggregate state instead of a derived expression.
- 2026-04-04: Fixed the time-windowed ClickHouse query to project `total_exec_time_ms` in the outer SELECT, and added a reset SQL file that truncates and rebuilds `query_fingerprints` plus `top_offenders_mv` for existing ClickHouse instances.
- 2026-04-04: Switched the ClickHouse read model to store `source_tag`, `source_file`, and `sample_query` directly so all-time `analyze_table` filtering stays correct even when the same fingerprint appears under different tags.
- 2026-04-05: Refined the ClickHouse grouping key to `fingerprint + source_tag` and moved `source_file` and `sample_query` back into aggregate state columns so all-time table-scoped ranking stays stable without fragmenting by sample SQL.
- 2026-04-05: Aligned the default `query_events` path with the same `fingerprint + source_tag` grouping so both ClickHouse query paths treat source tags consistently and keep `source_file` / `sample_query` as representatives.
- 2026-04-04: Added `DemoRepoTool` to apply the smallest concrete fix in the sibling demo repo, commit it on `agent/demo-fix`, and push before GitHub PR creation; the first live proof produced commits `4e68f5f`, `c2bcc5f`, and `c2fb3d5` in `/home/bjw/db-specialist-demo`.
- 2026-04-04: The live GitHub proof now succeeds when the env contract is present: `analyze_db` returned `rewrite_like`, `add_index`, and `rewrite_count` findings and opened `https://github.com/bwarminski/db-specialist-demo/pull/1`.
- 2026-04-05: Tightened the ClickHouse read model so both query paths group by `fingerprint + source_tag` and carry a single `representative_state` tuple for `source_file` and `sample_query`, preventing mismatched representative values from separate aggregate states.
- 2026-04-05: Updated the reset SQL to rebuild `query_fingerprints` and `top_offenders_mv` with the tuple representative state while keeping the stop-ingestion note intact.
- 2026-04-05: Updated `DemoRepoTool` to require a finding fingerprint, create a per-finding `agent/demo-fix-<fingerprint>` branch after a `git ls-remote origin` credential check, verify each rewrite actually changes the file, and return the branch name plus diff output.
- 2026-04-05: Added a deterministic hash suffix to `DemoRepoTool` branch names when sanitized fingerprints exceed 12 characters so long same-prefix findings no longer collide.
- 2026-04-05: Added retry-safe demo branch creation by deleting the local branch before recreating it from `origin/<baseRef>`, and made unsupported `fix_type` values fail fast instead of falling through to `add_index`.
- 2026-04-05: Switched demo branch recreation to `git checkout -B` so same-fingerprint retries stay valid in real git, and tightened `add_includes` so partial ternary rewrites fail as drift instead of committing half a change.
- 2026-04-05: Added README demo setup/reset guidance for the external Rails repo and captured the Phase 2 `pi-agent-core` loop plus honest post-fix validation follow-up in `TODOS.md`.
- 2026-04-05: Threaded the per-finding demo branch name and diff from `DemoRepoTool` through `DBSpecialistExecutor` into `GitHubTool`, and updated the PR body to include a fenced `## Code Change` section plus `## EXPLAIN (sample query)`.
- 2026-04-05: Removed the instance-level cached PR URL from `GitHubTool`, kept the 422 existing-PR lookup behavior, and made tokened GitHub calls fail fast when `DEMO_REPO` is missing.
- 2026-04-05: Brett approved replacing the untracked Phase 2 implementation plan instead of patching stale assumptions in place; the replacement plan now uses a separate architecture addendum, removes the ClickHouse backward-compatibility layer, replaces the SQL memory assumption with a hybrid markdown-plus-JSONL memory model for preferences, constraints, discoveries, and failed attempts, and switches the LLM config contract to provider-agnostic `LLM_MODEL`.
- 2026-04-05: Task 6 smoke verification found two contract drifts, not runtime regressions: the current plan's `message/send` curl example was missing the required A2A `messageId` field, and `tests/smoke/test_demo_repo_split.py` still expected `LLM_FALLBACK_MODELS` even though the approved Phase 2 contract is the single provider-qualified `LLM_MODEL`.
- 2026-04-05: Tightened the loop safety boundary by requiring run-local evidence before risky actions: `apply_fix` now needs a previously loaded high-severity finding, a validated query for that finding, and a source lookup, `open_pull_request` now needs prior validation plus a prepared fix from the same run, completed A2A payloads preserve structured tool results, and executor coverage now includes the real `pi-agent-core` Agent path via a local `streamFn`.
- 2026-04-05: Removed `DEMO_HEAD_REF` from the live runtime contract and docs because PR head branches now come from per-fingerprint preparation state, simplified the stdlib `.env` loaders to `split("=", 1)` semantics with no quoting/comment magic, fixed `readStringProperty()` to reject only `undefined`, documented that `pg_stat_statements.rows` is rows returned/affected rather than rows visited, and added both `scripts/validate.sh` and `agent/test/e2e/live_provider_validation.test.ts` for manual real-provider validation with graceful skips when provider credentials are absent.
