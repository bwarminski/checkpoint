# Investigation

Use `query_findings` first. Only call `query_database` for guarded follow-up queries.
Only continue to `apply_fix` when the selected finding is high severity and the query has validated output.
